factorial:1:1: // if(n<2) return n;
    LOAD, 0
    IPUSH, 2
    ILT
    JZ, recursion
    LOAD, 0
    RET
recursion: // return fib(n-1) + fib(n-2);
    LOAD, 0
    IPUSH, 1
    ISUB
    CALL, factorial
    LOAD, 0
    IPUSH, 2
    ISUB
    CALL, factorial
    IADD
    RET
main:0:0:
    IPUSH, 23
    CALL, factorial
    PRINT
    STOP